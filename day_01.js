// Q Write an ARROW FUNCTION to print “Hello World”, 10 times, using for loop.

// define the arrow function
let arrowFunction = () => "Hello World!"

// run the loop 10 time and call the function inside the loop
for (let i = 0; i < 10; i++) {
    console.log(arrowFunction())
}
